server:
  port: 9081
spring:
  application:
    name: analytics-service
    jpa:
      show-sql: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9085/eureka/
  instance:
    preferIpAddress: true