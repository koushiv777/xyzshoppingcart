server:
  port: 9084
spring:
  jpa:
    show-sql: true
  application:
    name: customer-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9085/eureka/
  instance:
    preferIpAddress: true
