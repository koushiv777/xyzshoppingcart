
server:
  port: 9080
spring:
  jpa:
    show-sql: true
  application:
    name: product-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9085/eureka/
  instance:
    preferIpAddress: true